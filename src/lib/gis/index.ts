/**
 * BHUSHAKTI AI — GIS & Geospatial Service Layer
 * Coordinates PostGIS vector queries, GeoJSON feature generators, and heatmap tile calculations.
 */

export interface GISLayerConfig {
  id: string;
  name: string;
  visible: boolean;
  opacity: number;
}
